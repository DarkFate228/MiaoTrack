<resources>
    <!-- ЛАЙТ-ТЕМА ПРИЛОЖЕНИЯ -->
    <style name="Theme.Glucose" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Цветовые роли Material 3 -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorSurface">@color/md_theme_light_surface</item>
        <item name="colorOnSurface">@color/md_theme_light_onSurface</item>
        <item name="colorSurfaceVariant">@color/md_theme_light_surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/md_theme_light_onSurfaceVariant</item>
        <item name="colorError">@color/md_theme_light_error</item>
        <item name="colorOnError">@color/md_theme_light_onError</item>

        <!-- Системные панели -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>

        <!-- Стандартизируем карточки/кнопки -->
        <item name="materialCardViewStyle">@style/Widget.App.Card</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <!-- Карточки по умолчанию (под твою белую card в блоке ввода) -->
    <style name="Widget.App.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="strokeColor">@color/md_theme_light_outline</item>
        <item name="strokeWidth">1dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardPreventCornerOverlap">true</item>
    </style>

    <!-- Кнопка по умолчанию = красная с белым текстом -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
        <!-- фон/текст подтянутся из colorPrimary/colorOnPrimary -->
    </style>

    <!-- Оверлей для TextInputLayout (обводка и хинт берут colorPrimary/OnSurface) -->
    <style name="ThemeOverlay.App" parent="">
        <item name="colorSurface">@color/md_theme_light_primaryContainer</item>
        <item name="colorOnSurface">@color/md_theme_light_onPrimaryContainer</item>
    </style>
</resources>
